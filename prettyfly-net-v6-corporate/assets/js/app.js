
const PF={products:window.PRETTYFLY_PRODUCTS||[],cart:JSON.parse(localStorage.getItem("pf-v6-cart")||"[]"),wish:JSON.parse(localStorage.getItem("pf-v6-wish")||"[]")};
const money=n=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(n);
const save=()=>{localStorage.setItem("pf-v6-cart",JSON.stringify(PF.cart));localStorage.setItem("pf-v6-wish",JSON.stringify(PF.wish));};
const stars=r=>"★★★★★".slice(0,Math.round(r))+"☆☆☆☆☆".slice(0,5-Math.round(r));
function toast(m){const t=document.getElementById("toast");if(!t)return;t.textContent=m;t.classList.add("show");clearTimeout(window.pft);window.pft=setTimeout(()=>t.classList.remove("show"),2100)}
function card(p){return `<article class="product-card"><a class="product-media" href="product.html?id=${p.id}"><span class="badge">${p.badge}</span><button class="wish ${PF.wish.includes(p.id)?"active":""}" data-wish="${p.id}">${PF.wish.includes(p.id)?"♥":"♡"}</button><img src="${p.image}" alt="${p.name}"></a><div class="product-info"><span class="meta">${p.label}</span><h3><a href="product.html?id=${p.id}">${p.name}</a></h3><div class="rating">${stars(p.rating)} <span>(${p.reviews})</span></div><div class="price-row"><div class="prices"><span class="price">${money(p.price)}</span>${p.oldPrice?`<span class="old-price">${money(p.oldPrice)}</span>`:""}</div><button class="add" data-add="${p.id}">ADD</button></div></div></article>`}
function wire(){document.querySelectorAll("[data-add]").forEach(b=>b.onclick=e=>{e.preventDefault();add(Number(b.dataset.add),1)});document.querySelectorAll("[data-wish]").forEach(b=>b.onclick=e=>{e.preventDefault();wish(Number(b.dataset.wish))})}
function add(id,qty=1){const p=PF.products.find(x=>x.id===id);if(!p)return;const row=PF.cart.find(x=>x.id===id);row?row.qty+=qty:PF.cart.push({id,qty});save();renderCart();toast(`${p.name} added to cart`)}
function wish(id){PF.wish=PF.wish.includes(id)?PF.wish.filter(x=>x!==id):[...PF.wish,id];save();renderAll();toast(PF.wish.includes(id)?"Saved to wishlist":"Removed from wishlist")}
function renderCart(){const count=PF.cart.reduce((s,x)=>s+x.qty,0);const badge=document.getElementById("cartCount");if(badge)badge.textContent=count;const items=document.getElementById("cartItems"),empty=document.getElementById("cartEmpty"),subtotal=document.getElementById("cartSubtotal");if(!items)return;items.innerHTML=PF.cart.map(ci=>{const p=PF.products.find(x=>x.id===ci.id);return p?`<div class="cart-item"><img src="${p.image}" alt="${p.name}"><div><h5>${p.name}</h5><small>${ci.qty} × ${money(p.price)}</small></div><button data-remove="${p.id}">×</button></div>`:""}).join("");empty.style.display=PF.cart.length?"none":"block";subtotal.textContent=money(PF.cart.reduce((s,ci)=>{const p=PF.products.find(x=>x.id===ci.id);return s+(p?p.price*ci.qty:0)},0));document.querySelectorAll("[data-remove]").forEach(b=>b.onclick=()=>{PF.cart=PF.cart.filter(x=>x.id!==Number(b.dataset.remove));save();renderCart()})}
function renderLists(){document.querySelectorAll("[data-list]").forEach(el=>{let list=[...PF.products],t=el.dataset.list;if(t==="new")list=list.filter(x=>x.new);if(t==="best")list=list.filter(x=>x.best);if(t==="deals")list=list.filter(x=>x.deal);if(t==="featured")list=list.filter(x=>x.best||x.new);el.innerHTML=list.slice(0,Number(el.dataset.limit||12)).map(card).join("")});wire()}
let SHOP_VISIBLE=16;
function shop(){const grid=document.getElementById("shopGrid");if(!grid)return;const params=new URLSearchParams(location.search),preset=params.get("category");if(preset&&!window._preset){document.querySelectorAll(`[data-filter-category][value="${preset}"]`).forEach(x=>x.checked=true);window._preset=true}const cats=[...document.querySelectorAll("[data-filter-category]:checked")].map(x=>x.value);let list=[...PF.products];if(cats.length)list=list.filter(p=>cats.includes(p.category));const specials=[...document.querySelectorAll("[data-filter-special]:checked")].map(x=>x.value);if(specials.includes("new"))list=list.filter(p=>p.new);if(specials.includes("best"))list=list.filter(p=>p.best);if(specials.includes("deal"))list=list.filter(p=>p.deal);const sort=document.getElementById("shopSort")?.value||"featured";if(sort==="price-low")list.sort((a,b)=>a.price-b.price);if(sort==="price-high")list.sort((a,b)=>b.price-a.price);if(sort==="name")list.sort((a,b)=>a.name.localeCompare(b.name));document.getElementById("productCount").textContent=`${list.length} products`;grid.innerHTML=list.slice(0,SHOP_VISIBLE).map(card).join("");wire();const more=document.getElementById("loadMore");if(more)more.style.display=SHOP_VISIBLE<list.length?"inline-flex":"none"}
function product(){const root=document.getElementById("productDetail");if(!root)return;const id=Number(new URLSearchParams(location.search).get("id")||1),p=PF.products.find(x=>x.id===id)||PF.products[0];document.title=`${p.name} | PrettyFly`;root.innerHTML=`<div class="breadcrumbs"><a href="index.html">Home</a> / <a href="shop.html">Products</a> / ${p.name}</div><div class="product-detail"><div class="product-detail__image"><img src="${p.image}" alt="${p.name}"></div><div class="product-detail__info"><p class="eyebrow">${p.label.toUpperCase()}</p><h1>${p.name}</h1><div class="rating">${stars(p.rating)} <span>${p.rating} · ${p.reviews} reviews</span></div><div class="product-detail__price">${money(p.price)}</div><p class="desc">${p.description}</p><div class="product-meta"><div><span>SKU</span><strong>${p.sku}</strong></div><div><span>Availability</span><strong>${p.stock}</strong></div><div><span>Returns</span><strong>30 days</strong></div><div><span>Support</span><strong>Mon–Fri</strong></div></div><div class="qty-row"><div class="qty"><button id="minus">−</button><input id="qty" value="1"><button id="plus">+</button></div><button class="btn btn-blue" id="productAdd">Add to Cart</button></div><div class="detail-actions"><button class="btn btn-outline" id="productWish">${PF.wish.includes(p.id)?"♥ Saved":"♡ Save"}</button><a class="btn btn-outline" href="contact.html">Ask Support</a></div><div class="notice"><strong>Need help?</strong> Call PrettyFly Support at +1 (202) 555-0147.</div></div></div>`;let q=1,input=document.getElementById("qty");document.getElementById("minus").onclick=()=>{q=Math.max(1,q-1);input.value=q};document.getElementById("plus").onclick=()=>{q++;input.value=q};document.getElementById("productAdd").onclick=()=>add(p.id,q);document.getElementById("productWish").onclick=()=>{wish(p.id);product()};const rel=document.getElementById("related");if(rel){rel.innerHTML=PF.products.filter(x=>x.category===p.category&&x.id!==p.id).slice(0,4).map(card).join("");wire()}}
function search(){const input=document.getElementById("globalSearch"),panel=document.getElementById("searchPanel"),results=document.getElementById("searchResults");if(!input)return;input.oninput=()=>{const q=input.value.trim().toLowerCase();if(!q){panel.classList.remove("open");return}const hits=PF.products.filter(p=>`${p.name} ${p.label}`.toLowerCase().includes(q)).slice(0,7);results.innerHTML=hits.length?hits.map(p=>`<a class="search-hit" href="product.html?id=${p.id}"><img src="${p.image}" alt=""><div><strong>${p.name}</strong><small>${p.label}</small></div><b>${money(p.price)}</b></a>`).join(""):`<div style="padding:16px;color:#64748B">No matching products.</div>`;panel.classList.add("open")};document.addEventListener("click",e=>{if(!e.target.closest(".search")&&!e.target.closest(".search-panel"))panel.classList.remove("open")})}
function initUI(){document.getElementById("cartBtn")?.addEventListener("click",()=>{document.getElementById("cartDrawer").classList.add("open");document.body.classList.add("lock")});document.getElementById("cartClose")?.addEventListener("click",()=>{document.getElementById("cartDrawer").classList.remove("open");document.body.classList.remove("lock")});document.getElementById("cartOverlay")?.addEventListener("click",()=>document.getElementById("cartClose").click());document.getElementById("checkoutBtn")?.addEventListener("click",()=>toast("Demo checkout — connect a payment provider before launch."));const menu=document.getElementById("menuBtn"),nav=document.getElementById("mainNav");if(menu)menu.onclick=()=>nav.classList.toggle("open");const modal=document.getElementById("accountModal");document.getElementById("accountBtn")?.addEventListener("click",()=>modal.classList.add("open"));document.getElementById("accountClose")?.addEventListener("click",()=>modal.classList.remove("open"));document.getElementById("demoSignIn")?.addEventListener("click",()=>{modal.classList.remove("open");toast("Demo sign-in only")});document.getElementById("wishlistBtn")?.addEventListener("click",()=>toast(`${PF.wish.length} saved item${PF.wish.length===1?"":"s"}`));document.getElementById("newsletterForm")?.addEventListener("submit",e=>{e.preventDefault();const em=document.getElementById("newsletterEmail");document.getElementById("newsletterMsg").textContent=`Thanks! ${em.value} is subscribed.`;em.value=""});document.querySelectorAll(".faq-q").forEach(q=>q.onclick=()=>q.closest(".faq-item").classList.toggle("open"));document.querySelectorAll("[data-filter-category],[data-filter-special]").forEach(x=>x.onchange=()=>{SHOP_VISIBLE=16;shop()});document.getElementById("shopSort")?.addEventListener("change",shop);document.getElementById("clearFilters")?.addEventListener("click",()=>{document.querySelectorAll(".filters input").forEach(x=>x.checked=false);history.replaceState({}, "", "shop.html");SHOP_VISIBLE=16;shop()});document.getElementById("loadMore")?.addEventListener("click",()=>{SHOP_VISIBLE+=16;shop()});document.getElementById("filterToggle")?.addEventListener("click",()=>document.getElementById("filters")?.classList.toggle("open"));const form=document.getElementById("contactForm");if(form)form.onsubmit=e=>{e.preventDefault();form.reset();toast("Demo support request submitted")};const track=document.getElementById("trackForm");if(track)track.onsubmit=e=>{e.preventDefault();document.getElementById("trackResult").classList.add("show")}}
function renderAll(){renderLists();shop();product();renderCart()}
document.addEventListener("DOMContentLoaded",()=>{renderAll();search();initUI()});

document.addEventListener("DOMContentLoaded",()=>{
 const tf=document.getElementById("trackForm");
 if(tf) tf.addEventListener("submit",e=>{
   e.preventDefault();
   const r=document.getElementById("trackResult");
   r.style.display="block";
   r.innerHTML='<div class="notice"><strong>Demo status:</strong> Order processing. Connect your fulfillment system for live tracking.</div>';
 });
});
